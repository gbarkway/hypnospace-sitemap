version: '3.4'

services:
  captureserv:
    image: captureserv
    build:
      context: .
      target: production
    environment:
      NODE_ENV: production
    ports:
      - 3000:3000
      
