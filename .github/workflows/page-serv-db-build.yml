# based on https://github.com/do-community/example-doctl-action/blob/master/.github/workflows/workflow.yaml
name: page-serv-db-build

on:
  push:
    branches:
      - main
    paths:
      - 'src/page-serv/db/**'
      - '.github/workflows/page-serv-db-build.yml'

jobs:

  build:
    name: Build and push image
    runs-on: ubuntu-latest
    steps:

    - name: Checkout master
      uses: actions/checkout@master

    - name: Build container image
      run: docker build -t registry.digitalocean.com/gbarkway/hs-pageserv-db:$(echo $GITHUB_SHA | head -c7) -t registry.digitalocean.com/gbarkway/hs-pageserv-db:latest ./src/page-serv/db
    
    - name: Install doctl
      uses: digitalocean/action-doctl@v2
      with:
        token: ${{ secrets.DO_ACCESS_TOKEN }}

    - name: Log in to DigitalOcean Container Registry with short-lived credentials
      run: doctl registry login --expiry-seconds 600

    - name: Push image to DigitalOcean Container Registry
      run: docker push -a registry.digitalocean.com/gbarkway/hs-pageserv-db

    - name: Display new tag
      run: echo "Pushed registry.digitalocean.com/gbarkway/hs-pageserv-db:$(echo $GITHUB_SHA | head -c7) and registry.digitalocean.com/gbarkway/hs-pageserv-db:latest"
