name: capture-serv test

on:
  push:
    branches: [ main ]
    paths:
      - 'src/capture-serv/**'
  pull_request:
    branches: [ main ]
    paths: 
      - 'src/capture-serv/**'

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Run docker-compose
      run: docker-compose -f ./src/capture-serv/docker-compose.yml -f ./src/capture-serv/docker-compose.debug.yml run captureserv npm test
